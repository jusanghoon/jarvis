<Page x:Class="javis.Pages.UserProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Grid Margin="14">
        <Border CornerRadius="16"
                Background="#22000000"
                BorderBrush="#33BFE9FF"
                BorderThickness="1"
                Padding="16">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,12">
                    <Button Content="← 뒤로가기" Click="Back_Click" Padding="12,6"/>
                    <TextBlock Text="User 프로필" Foreground="#EAF6FF" FontSize="18" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="12,0,0,0"/>
                </DockPanel>

                <Border Grid.Row="1" CornerRadius="12"
                        Background="#11FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1"
                        Padding="14" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="ID" Foreground="#A8C7D9"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Id}" Foreground="#EAF6FF"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,10,0,0" Text="Name" Foreground="#A8C7D9"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,10,0,0"
                                 Height="36" VerticalContentAlignment="Center"
                                 Background="#F2FFFFFF" Foreground="#0B1A2A" BorderBrush="#22FFFFFF"
                                 Padding="10,6"
                                 Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                </Border>

                <!-- Report / editable profile fields -->
                <Border Grid.Row="2" CornerRadius="12"
                        Background="#11000000" BorderBrush="#22FFFFFF" BorderThickness="1"
                        Padding="14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="프로필 보고서 (키:값)" Foreground="#EAF6FF" FontSize="14" FontWeight="SemiBold"/>

                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
                            <Button Content="자동 채우기" Click="AutoFill_Click" Padding="12,6"/>
                            <Button Content="TSV 내보내기" Click="ExportTsv_Click" Padding="12,6" Margin="10,0,0,0"/>
                        </StackPanel>

                        <TextBox x:Name="ReportBox"
                                 Grid.Row="2"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Background="#22FFFFFF"
                                 BorderBrush="#22FFFFFF"
                                 Foreground="#EAF6FF"
                                 Padding="12"
                                 FontFamily="Consolas, Segoe UI"
                                 FontSize="14"/>
                    </Grid>
                </Border>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                    <Button Content="저장" Click="Save_Click" Padding="14,8" MinWidth="90"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
