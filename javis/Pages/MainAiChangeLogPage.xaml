<Page x:Class="javis.Pages.MainAiChangeLogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Grid Margin="14">
        <Border CornerRadius="16"
                Background="#22000000"
                BorderBrush="#33BFE9FF"
                BorderThickness="1"
                Padding="16">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,12">
                    <Button Content="← 뒤로가기" Click="Back_Click" Padding="12,6"/>
                    <TextBlock Text="Main AI 변경 로그" Foreground="#EAF6FF" FontSize="18" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="12,0,0,0"/>
                </DockPanel>

                <TextBlock Grid.Row="1" Text="최근 Main AI가 프로필에 추가한 항목" Foreground="#A8C7D9" Margin="0,0,0,8"/>

                <TextBox Grid.Row="2" x:Name="LogBox"
                         IsReadOnly="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Background="#11000000"
                         BorderBrush="#22FFFFFF"
                         Foreground="#EAF6FF"
                         Padding="12"
                         FontFamily="Consolas, Segoe UI"
                         FontSize="13"/>
            </Grid>
        </Border>
    </Grid>
</Page>
