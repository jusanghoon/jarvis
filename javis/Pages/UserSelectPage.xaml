<Page x:Class="javis.Pages.UserSelectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Grid Margin="14">
        <Border CornerRadius="16"
                Background="#22000000"
                BorderBrush="#33BFE9FF"
                BorderThickness="1"
                Padding="16">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,12">
                    <Button Content="← 뒤로가기" Click="Back_Click" Padding="12,6"/>
                    <TextBlock Text="User 선택" Foreground="#EAF6FF" FontSize="18" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="12,0,0,0"/>
                </DockPanel>

                <Border Grid.Row="1" CornerRadius="12"
                        Background="#11FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1"
                        Padding="14" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="NewUserNameBox"
                                 Height="36"
                                 VerticalContentAlignment="Center"
                                 Background="#F2FFFFFF"
                                 Foreground="#0B1A2A"
                                 BorderBrush="#22FFFFFF"
                                 Padding="10,6"
                                 Text="" />

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                            <Button Content="추가" Click="AddUser_Click" Padding="12,6"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <ListBox x:Name="UserList" Grid.Row="2" BorderThickness="0" Background="Transparent"
                         Foreground="#EAF6FF" SelectionChanged="UserList_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="12" Padding="12" Margin="0,6"
                                    Background="#11000000" BorderBrush="#22FFFFFF" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock Text="{Binding DisplayName}" FontSize="16" FontWeight="SemiBold" Foreground="#EAF6FF"/>
                                        <TextBlock Text="{Binding Id}" FontSize="12" Opacity="0.7" Foreground="#EAF6FF"/>
                                    </StackPanel>

                                    <Button Grid.Column="1" Content="수정" Padding="12,6" Margin="12,0,0,0"
                                            Click="EditUser_Click"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Page>
