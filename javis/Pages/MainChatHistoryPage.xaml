<Page x:Class="javis.Pages.MainChatHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent"
      Title="MainChatHistory">

    <Page.Resources>
        <!-- 기본은 밝은 내용 텍스트 (리스트/상세/헤더) -->
        <Style x:Key="ContentText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EAF6FF"/>
        </Style>

        <!-- 버튼은 밝은 배경 위에서 읽히도록 검정 텍스트로 -->
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#000000"/>
        </Style>
    </Page.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <Button Content="← 돌아가기" Click="Back_Click" Padding="12,6"/>
            <TextBlock Text="메인 채팅 이전 기록" FontSize="18" FontWeight="SemiBold"
                       Margin="12,0,0,0" VerticalAlignment="Center"
                       Style="{StaticResource ContentText}"/>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="열기" Click="Open_Click" Margin="0,0,8,0" Padding="12,6"/>
                    <Button Content="삭제" Click="Delete_Click" Padding="12,6"/>
                </StackPanel>

                <ListView Grid.Row="1" x:Name="SessionList" MouseDoubleClick="OpenSelected_DoubleClick"
                          SelectionChanged="SessionList_SelectionChanged"
                          Background="Transparent" BorderThickness="0"
                          Foreground="#EAF6FF">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,6,0,6">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"
                                           Style="{StaticResource ContentText}"/>
                                <TextBlock Grid.Row="1" Text="{Binding CreatedAtText}" Opacity="0.7" FontSize="12"
                                           Style="{StaticResource ContentText}"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <GridSplitter Grid.Column="1" Width="10" HorizontalAlignment="Stretch"/>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="DetailTitle" Grid.Row="0" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"
                           Style="{StaticResource ContentText}"/>

                <ListBox x:Name="DetailLines"
                         Grid.Row="1"
                         Background="Transparent"
                         BorderThickness="0"
                         HorizontalContentAlignment="Stretch"
                         HorizontalAlignment="Stretch"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.Resources>
                        <Style TargetType="ScrollViewer">
                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        </Style>
                    </ListBox.Resources>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel HorizontalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,6" HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Assistant/System (left) -->
                                <Border Grid.Column="0"
                                        CornerRadius="12"
                                        Padding="12,8"
                                        MaxWidth="520"
                                        Background="#2B2F36"
                                        BorderBrush="#3A3F47"
                                        BorderThickness="1"
                                        HorizontalAlignment="Left"
                                        Margin="0,0,12,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAssistant}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <StackPanel>
                                        <TextBlock Text="Jarvis" Opacity="0.8" FontSize="12" Margin="0,0,0,4" Foreground="#EAF6FF"/>
                                        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Foreground="#EAF6FF"/>
                                    </StackPanel>
                                </Border>

                                <!-- User (right) -->
                                <Border Grid.Column="1"
                                        CornerRadius="12"
                                        Padding="12,8"
                                        MaxWidth="520"
                                        Background="#1F3A5F"
                                        BorderBrush="#2B5EA8"
                                        BorderThickness="1"
                                        HorizontalAlignment="Right"
                                        Margin="0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <StackPanel>
                                        <TextBlock Text="You" Opacity="0.9" FontSize="12" Margin="0,0,0,4" Foreground="#EAF6FF"/>
                                        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Foreground="#EAF6FF"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</Page>
