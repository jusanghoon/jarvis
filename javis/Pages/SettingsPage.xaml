<Page x:Class="javis.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:services="clr-namespace:javis.Services"
      Background="Transparent">

    <Grid Margin="14">
        <Border CornerRadius="16"
                Background="#22000000"
                BorderBrush="#33BFE9FF"
                BorderThickness="1"
                Padding="16">

            <StackPanel>

                <TextBlock Text="Settings" Foreground="#EAF6FF" FontSize="18" FontWeight="SemiBold"/>

                <!-- User -->
                <Border Margin="0,14,0,0" CornerRadius="12"
                        Background="#11FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1"
                        Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="User"
                                   Foreground="#A8C7D9"
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="0" Grid.Column="1"
                                   Text="{Binding ActiveUserName, Source={x:Static Member=services:UserProfileService.Instance}}"
                                   Foreground="#EAF6FF"
                                   VerticalAlignment="Center"/>

                        <Button Grid.Row="0" Grid.Column="2"
                                Content="선택"
                                Click="UserSelect_Click"
                                Margin="10,0,0,0"
                                Padding="12,6"/>

                        <TextBlock Grid.Row="1" Grid.Column="0"
                                   Margin="0,10,0,0"
                                   Text="Note"
                                   Foreground="#A8C7D9"
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                                   Margin="0,10,0,0"
                                   Text="유저별로 Jarvis 설정/기록을 분리할 수 있습니다."
                                   Foreground="#EAF6FF"/>
                    </Grid>
                </Border>

                <Border Margin="0,14,0,0" CornerRadius="12"
                        Background="#11FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1"
                        Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Model"
                                   Foreground="#A8C7D9"
                                   VerticalAlignment="Center"/>

                        <ComboBox Grid.Row="0" Grid.Column="1"
                                  Height="36"
                                  Background="#F2FFFFFF"
                                  Foreground="#0B1A2A"
                                  BorderBrush="#22FFFFFF"
                                  SelectedValuePath="Content"
                                  SelectedValue="{Binding Model, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <ComboBoxItem Content="qwen3:4b" Foreground="#0B1A2A"/>
                            <ComboBoxItem Content="qwen3:8b" Foreground="#0B1A2A"/>
                        </ComboBox>

                        <TextBlock Grid.Row="1" Grid.Column="0"
                                   Margin="0,10,0,0"
                                   Text="Hotkeys"
                                   Foreground="#A8C7D9"
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="1" Grid.Column="1"
                                   Margin="0,10,0,0"
                                   Text="Ctrl+1 = qwen3:4b / Ctrl+2 = qwen3:8b"
                                   Foreground="#EAF6FF"/>
                    </Grid>
                </Border>

            </StackPanel>
        </Border>
    </Grid>
</Page>
